### Topic 2.4 — Nested if Statements

#### 1
Consider the following code segment:
```java
int x = 7;
if (x > 5) {
if (x < 10) {
System.out.print("A");
} else {
System.out.print("B");
}
} else {
System.out.print("C");
}
```
What is printed?

- A) A
- B) B
- C) C
- D) AB
- E) Nothing

Answer: A

#### 2
Consider the following code segment:
```java
int n = 3;
if (n % 2 == 0) {
if (n > 0) {
System.out.print("even-positive");
}
} else {
System.out.print("odd");
}
```
What is printed?

- A) even-positive
- B) odd
- C) even-positiveodd
- D) Nothing
- E) Compilation error

Answer: B

#### 3
In a nested if statement, when is the **inner** Boolean expression evaluated?

- A) Only if the outer expression is false.
- B) Only if the outer expression is true.
- C) It is always evaluated first.
- D) It is evaluated regardless of the outer condition.
- E) Only if the program compiles without warnings.

Answer: B

#### 4
Which output is produced by the following code?
```java
int a = 4;
int b = 9;
if (a > 5) {
if (b > 5) {
System.out.print("X");
} else {
System.out.print("Y");
}
} else {
if (b > 5) {
System.out.print("Z");
}
}
```

- A) X
- B) Y
- C) Z
- D) XZ
- E) Nothing

Answer: C

#### 5
Consider the following nested if statement:
```java
String s = "code";
if (s.length() > 3) {
if (s.substring(0, 2).equals("co")) {
System.out.print("match");
}
}
```
What is printed?

- A) code
- B) match
- C) co
- D) Nothing
- E) Compilation error

Answer: B

#### 6
What is the value of result after the following code executes?
```java
int result = 0;
int val = -2;
if (val < 0) {
if (val < -5) {
result = 1;
} else {
result = 2;
}
} else {
result = 3;
}
```

- A) 0
- B) 1
- C) 2
- D) 3
- E) -2

Answer: C

#### 7
Which statement best describes the behavior of the following code?
```java
int x = 2;
int y = 3;
if (x > 0)
if (y > 0)
System.out.print("Q");
else
System.out.print("R");
```

- A) It always prints Q.
- B) It always prints R.
- C) It prints Q because the else matches the inner if.
- D) It prints R because the else matches the outer if.
- E) It prints nothing.

Answer: C

#### 8
Which of the following is a correct description of a **multi-way selection** (if-else-if)?

- A) Every block whose condition is true will execute.
- B) Only the else block is guaranteed to execute.
- C) No more than one segment of code is executed based on the first expression that evaluates to true.
- D) It repeats code until a condition is met.
- E) It can only be used with String objects.

Answer: C

#### 9
Consider the following code segment:
```java
int x = 1;
int y = 2;
String z = "Z";
if (x + y > 2) {
if (z.equals("Z")) {
System.out.print(x + y + z);
}
}
```
What is printed?

- A) 12Z
- B) 3Z
- C) 12zee
- D) 3zee
- E) onetwozee

Answer: B

#### 10
Which call correctly returns "Renaissance" for years 1400 to 1600 inclusive in this nested-if method?
```java
public static String getCategory(int year) {
String category = "Other";
if (year >= 1400) {
if (year <= 1600) {
category = "Renaissance";
}
}
return category;
}
```

- A) getCategory(1399)
- B) getCategory(1400)
- C) getCategory(1601)
- D) getCategory(1000)
- E) getCategory(1700)

Answer: B
