### Topic 4.8 — ArrayList Methods

#### 1
When producing statistics for long lists of numerical data, why use int[] instead of ArrayList?

- A) An array of primitives is more efficient to manipulate than a list of wrapper objects.
- B) Insertion into an array is easier to code.
- C) Removal from an array is easier to code.
- D) ArrayLists do not support random access.
- E) Arrays automatically double in size.

Answer: A

#### 2
Which ArrayList method returns the number of elements in the list?

- A) length()
- B) size()
- C) count()
- D) get()
- E) capacity()

Answer: B

#### 3
Given:
```java
ArrayList<String> list = new ArrayList<>();
list.add("a");
list.add(0, "b");
```
What is list.get(1)?

- A) "a"
- B) "b"
- C) null
- D) "ab"
- E) It throws an exception.

Answer: A

#### 4
What is the return value of list.remove(int index)?

- A) void
- B) boolean
- C) The element formerly at the specified position.
- D) The new size of the list.
- E) An Integer representing the index.

Answer: C

#### 5
Which method replaces an element in an ArrayList and returns the **old** element?

- A) add(index, obj)
- B) get(index)
- C) set(index, obj)
- D) remove(index)
- E) insert(index, obj)

Answer: C

#### 6
What is the index of the **first** element in an ArrayList?

- A) 1
- B) 0
- C) -1
- D) list.size()
- E) It depends on the JVM.

Answer: B

#### 7
Which method checks for an object's existence using .equals()?

- A) add
- B) remove
- C) get
- D) contains
- E) set

Answer: D

#### 8
Which statement about ArrayList generic types is true?

- A) They can only store primitive int values.
- B) They allow the compiler to find errors that would otherwise be found at runtime.
- C) They are less efficient than non-generic lists.
- D) They cannot be used with String.
- E) Generics are optional and ignored by the compiler.

Answer: B

#### 9
What exception occurs if you access list.get(list.size())?

- A) ArrayIndexOutOfBoundsException
- B) IndexOutOfBoundsException
- C) NullPointerException
- D) ConcurrentModificationException
- E) ArithmeticException

Answer: B
