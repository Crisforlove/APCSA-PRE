### Topic 4.9 — ArrayList Traversals

#### 1
The reviseList method is called with [3, 32, 33]. If it increments every odd number by 1, what is the final list?

- A) [3, 33]
- B) [3, 32, 33, 35]
- C) [32, 35]
- D) [32, 33, 35]
- E) [3, 32, 33]

Answer: C

#### 2
A loop removes all strings of length 3 from ["cat", "dog", "fish", "bat"] using i++. What is the result?

- A) ["fish"]
- B) ["dog", "fish"]
- C) ["cat", "dog", "bat"]
- D) ["dog", "fish", "bat"]
- E) ["fish", "bat"]

Answer: D

#### 3
What happens if you attempt to remove an element while traversing with an **enhanced for loop**?

- A) The element is successfully removed.
- B) The loop terminates immediately without error.
- C) A ConcurrentModificationException is thrown.
- D) The list size is updated correctly.
- E) An IndexOutOfBoundsException occurs.

Answer: C

#### 4
To safely remove elements while traversing, which object provides a remove() method that does **not** skip elements?

- A) Scanner
- B) File
- C) Iterator
- D) Integer
- E) String

Answer: C

#### 5
What is the correct way to iterate through an ArrayList from **last to first**?

- A) for (int i = 0; i < list.size(); i++)
- B) for (E obj : list)
- C) for (int i = list.size() - 1; i >= 0; i--)
- D) while (list.hasNext())
- E) for (int i = list.size(); i > 0; i++)

Answer: C

#### 6
Which statement about a for-each loop variable in an ArrayList is true?

- A) Assigning a new value to the variable changes the value in the list.
- B) The variable holds a copy of the reference to the element.
- C) The variable can be used to change the ArrayList size.
- D) The variable is always an int representing the index.
- E) The variable must be static.

Answer: B

#### 7
When deleting with a standard for loop (i++), how do you avoid skipping elements?

- A) Use an enhanced for loop.
- B) Manually decrement the index i-- after removal.
- C) Clear the entire list first.
- D) Use a while loop that never increments.
- E) It is impossible.

Answer: B

#### 8
Which line correctly prints names using for (Address addr : list)?

- A) System.out.println(list[addr].getName());
- B) System.out.println(Address.getName());
- C) System.out.println(addr.getName());
- D) System.out.println(list.getName());
- E) System.out.println(addr);

Answer: C

#### 9
A method uses an enhanced for loop to find the max ID. What is the **scope** of the loop variable student?

- A) The entire class.
- B) The whole method.
- C) Only the block of the enhanced for loop.
- D) Any subclass.
- E) Only the main method.

Answer: C

#### 10
Why does the enhanced for loop **not** update a primitive value in ArrayList?

- A) ArrayLists are immutable.
- B) The variable is assigned a copy of the element; changing it does not affect the list.
- C) Primitives cannot be stored in ArrayLists.
- D) Wrapper classes do not allow modification.
- E) Integer objects are static.

Answer: B
