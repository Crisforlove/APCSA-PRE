根据 **the sources**（特别是 AP CSA 2025 考纲 CED 和 Unit 4 题目集），以下是 **Topic 4.6 至 4.10** 的完整练习题集。 每小节包含 10 道题目，**优先采用真题（标注 Source）**，并确保涵盖代码追踪、逻辑计算与索引偏移等高难度考点。选项已按要求换行排版。 \--- \#\#\# **Topic 4.6 — Using Text Files** **题目数量：10** \#\#\#\# 1 (真题改编) A text file roster.txt contains: Cohen-Isabel, Lee-Cindy, Patel-Anisha. A segment is intended to assign \["ICohen", "CLee", "APatel"\] to usernames. Which code replaces /\* missing code \*/ correctly? 1, 2 A) while (scan.hasNext()) { String temp \= scan.next().substring(0, 1); usernames.add(temp \+ scan.next()); } B) while (scan.hasNext()) { String one \= scan.next(); String two \= scan.next(); usernames.add(two.substring(0, 1\) \+ one); } C) while (scan.hasNext()) { String\[\] temp \= scan.next().split("-"); usernames.add(temp\[3\].substring(0, 1\) \+ temp); } D) while (scan.hasNext()) { String\[\] temp \= scan.next().split("-"); usernames.add(temp.substring(0, 1\) \+ temp\[3\]); } E) while (scan.hasNext()) { usernames.add(scan.next().split("-")); } **Answer:** C 4 \#\#\#\# 2 (真题) A program evaluates arithmetic expressions read from a file. If the programmer forgot to check for **division by zero**, when will this error be detected? 5, 6 A) At compile time. B) While editing the program. C) As soon as the data from the input file is read. D) During evaluation of the expressions at runtime. E) When at least one incorrect value is output. **Answer:** D 5 \#\#\#\# 3 What is the most reliable way to count the number of lines in a text file using a Scanner? 6, 7 A) Use a while loop with hasNextLine() and a counter incremented for each nextLine(). B) Use hasNextInt() and check the file size. C) Access the sc.length() attribute directly. D) Read the entire file into a primitive array first. E) Use sc.hasNext() in a for loop from 0 to Integer.MAX\_VALUE. **Answer:** A 6 \#\#\#\# 4 Which of the following occurs if a programmer fails to call close() on a Scanner linked to a file? 7, 8 A) A compile-time error occurs. B) File contents are automatically deleted. C) A resource leak occurs as the file handle remains occupied. D) The JVM immediately shuts down. E) The file becomes read-only for all other programs. **Answer:** C 8 \#\#\#\# 5 How can a programmer skip the header line of a CSV file when reading it? 7, 8 A) Call nextLine() once before entering the reading loop. B) Use a regular expression filter on every line. C) Call sc.next(0) to reset the pointer. D) Convert the file to an array and delete index 0\. E) It is impossible to skip lines using only Scanner. **Answer:** A 8 \#\#\#\# 6 If new File("data/input.txt") results in a "File Not Found" error, what is the most likely cause? 8, 9 A) The file is larger than 1MB. B) The relative path is incorrect for the current working directory. C) Java files must end with the .java extension. D) Java does not support subdirectories. E) The Scanner object must be created before the File object. **Answer:** B 8 \#\#\#\# 7 Which Java class is most appropriate for **writing** text data to a file line-by-line? 8 A) FileInputStream B) PrintWriter or FileWriter C) System.out D) Scanner E) Math **Answer:** B 10 \#\#\#\# 8 What exception is thrown by new Scanner(new File("missing.txt")) if the file does not exist? 10, 11 A) IOException B) FileNotFoundException C) NullPointerException D) ArithmeticException E) No error is thrown until nextLine() is called. **Answer:** B 10 \#\#\#\# 9 To count the frequency of **individual words** in a text file, which Scanner method is best? 7, 10 A) nextLine() B) nextInt() C) next() D) nextDouble() E) hasNextLine() **Answer:** C 10 \#\#\#\# 10 How does nextLine() behave when reading a file with \\r\\n (Windows) line endings? 7, 10 A) It requires manual removal of the \\r character. B) It automatically handles common line-ending sequences and returns the string without them. C) It cannot read the file properly. D) It throws a RuntimeError. E) It treats \\r as part of the next line's data. **Answer:** B 12 \--- \#\#\# **Topic 4.7 — Wrapper Classes** **题目数量：10** \#\#\#\# 1 What is the result of d1 \+ d2 \+ d3.doubleValue() \+ d4 if the four Double objects sum to 100.0? 12, 13 A) 100.0 B) "100.0" C) 100 D) A compile-time error. E) A memory address. **Answer:** A 12 \#\#\#\# 2 Given Integer val \= 10;, what is printed after Integer r2 \= timesTwo(val); executes (assume timesTwo returns int 20)? 12, 14 A) 10 B) 20 C) 0 D) null E) A syntax error due to autoboxing. **Answer:** B 12 \#\#\#\# 3 Given Double y \= x / 2.0; y /= 2; where x is 3\. What does y.doubleValue() return? 12, 15 A) 0.0 B) 0.5 C) 0.75 D) 1.0 E) 1.5 **Answer:** C 16 \#\#\#\# 4 Which code segment correctly stores a random integer between 1 and 10 inclusive into an Integer object? 16, 17 A) (int)(Math.random()) \* 10 B) (int)(Math.random()) \* 10 \+ 1 C) (int)(Math.random() \* 10\) D) (int)(Math.random() \* 10\) \+ 1 E) (int)(Math.random() \+ 1\) \* 10 **Answer:** D 16 \#\#\#\# 5 Is the assignment Double d \= 20.0; double x \= d; legal in the AP Java subset? 16, 18 A) No, it requires an explicit cast. B) Yes, it is legal due to automatic unboxing. C) Yes, but only if x is initialized to 0\. D) No, Double cannot be converted to double. E) No, it causes a precision loss error. **Answer:** B 16 \#\#\#\# 6 Comparing two Integer objects a and b (value 128\) using a \== b may return false because: 16 A) The values are different. B) \== compares memory references, not content, for objects outside the cache range. C) Integer does not support \==. D) One object is automatically unboxed. E) 128 is a prime number in Java logic. **Answer:** B 16 \#\#\#\# 7 Which is the preferred method to obtain an Integer from an int for efficiency? 16 A) new Integer(5) B) Integer.valueOf(5) C) Integer.parseInt(5) D) Integer.toString(5) E) (Integer) 5 **Answer:** B 16 \#\#\#\# 8 Integer.parseInt("42") returns an int; what does Integer.valueOf("42") return? 16, 19 A) int B) Integer object C) String D) double E) void **Answer:** B 16 \#\#\#\# 9 When rounding a **negative** double d to the nearest integer, which formula is correct? 20, 21 A) (int)(d \+ 0.5) B) (int)(d \- 0.5) C) Math.abs(d) D) (int)d E) Math.round(d) **Answer:** B 20 \#\#\#\# 10 Why is 0.1 \+ 0.2 \== 0.3 potentially false in Java? 20 A) Double objects are immutable. B) Round-off errors occur in floating-point representations. C) The \+ operator is not for doubles. D) Types must be BigDecimal. E) It is always true. **Answer:** B 20 \--- \#\#\# **Topic 4.8 — ArrayList Methods** **题目数量：10** \#\#\#\# 1 (真题思路) When producing statistics for long lists of numerical data, why use int\[\] instead of ArrayList? 20, 22 A) An array of primitives is more efficient to manipulate than a list of wrapper objects. B) Insertion into an array is easier to code. C) Removal from an array is easier to code. D) ArrayLists do not support random access. E) Arrays automatically double in size. **Answer:** A 23 \#\#\#\# 2 Which code prints each name using a loop variable a from Address\[\] list? 23, 24 A) System.out.println(Address\[i\].getName()); B) System.out.println(list\[i\].getName()); C) System.out.println(a\[i\].getName()); D) System.out.println(a.getName()); E) System.out.println(list.getName()); **Answer:** D 23 \#\#\#\# 3 Given ArrayList allStudents, which code correctly prints each student's address name? 23, 25 A) System.out.println(allStudents.getName()); B) System.out.println(student.getName()); C) System.out.println(student.getAddress().getName()); D) System.out.println(allStudents.get(i).getName()); E) System.out.println(student\[i\].getName()); **Answer:** C 23 \#\#\#\# 4 What is the return value of list.remove(int index)? 26, 27 A) void B) boolean C) The element formerly at the specified position. D) The new size of the list. E) An Integer representing the index. **Answer:** C 26 \#\#\#\# 5 Which method replaces an element in an ArrayList and returns the **old** element? 26, 27 A) add(index, obj) B) get(index) C) set(index, obj) D) remove(index) E) insert(index, obj) **Answer:** C 26 \#\#\#\# 6 What is the index of the **first** element in an ArrayList? 26, 28 A) 1 B) 0 C) \-1 D) list.size() E) It depends on the JVM. **Answer:** B 26 \#\#\#\# 7 Which method checks for an object's existence using .equals()? 26, 29 A) add B) remove C) get D) contains E) set **Answer:** D 26 \#\#\#\# 8 (代码追踪) Given ArrayList nums containing , what are the final contents after \`nums.add(1, nums.remove(2));\`? \[23, 30\] A)  B)  C)  D)  E)  **Answer:** A 26 \#\#\#\# 9 Which statement about ArrayList generic types is true? 31 A) They can only store primitive int values. B) They allow the compiler to find errors that would otherwise be found at runtime. C) They are less efficient than non-generic lists. D) They cannot be used with String. E) Generics are optional and ignored by the compiler. **Answer:** B 31 \#\#\#\# 10 What exception occurs if you access list.get(list.size())? 26, 27 A) ArrayIndexOutOfBoundsException B) IndexOutOfBoundsException C) NullPointerException D) ConcurrentModificationException E) ArithmeticException **Answer:** B 26 \--- \#\#\# **Topic 4.9 — ArrayList Traversals** **题目数量：10** \#\#\#\# 1 (真题) The reviseList method is called with \[3, 32, 33\]. If it increments every odd number by 1, what is the final list? 34 A) \[3, 33\] B) \[3, 32, 33, 35\] C) \[32, 35\] D) \[32, 33, 35\] E) \[3, 32, 33\] **Answer:** C 4 \#\#\#\# 2 (索引偏移真题) A loop removes all strings of length 3 from \["cat", "dog", "fish", "bat"\] using i++. What is the result? 36, 37 A) \["fish"\] B) \["dog", "fish"\] C) \["cat", "dog", "bat"\] D) \["dog", "fish", "bat"\] E) \["fish", "bat"\] **Answer:** D (跳过了 "dog") 36 \#\#\#\# 3 (异常判定) What happens if you attempt to remove an element while traversing with an **enhanced for loop**? 38, 39 A) The element is successfully removed. B) The loop terminates immediately without error. C) A ConcurrentModificationException is thrown. D) The list size is updated correctly. E) An IndexOutOfBoundsException occurs. **Answer:** C 37 \#\#\#\# 4 To safely remove elements while traversing, which object provides a remove() method that does **not** skip elements? 40 A) Scanner B) File C) Iterator D) Integer E) String **Answer:** C 40 \#\#\#\# 5 What is the correct way to iterate through an ArrayList from **last to first**? 37 A) for (int i \= 0; i \< list.size(); i++) B) for (E obj : list) C) for (int i \= list.size() \- 1; i \>= 0; i--) D) while (list.hasNext()) E) for (int i \= list.size(); i \> 0; i++) **Answer:** C 37 \#\#\#\# 6 Which statement about a for-each loop variable in an ArrayList is true? 41, 42 A) Assigning a new value to the variable changes the value in the list. B) The variable holds a copy of the reference to the element. C) The variable can be used to change the ArrayList size. D) The variable is always an int representing the index. E) The variable must be static. **Answer:** B 40 \#\#\#\# 7 When deleting with a standard for loop (i++), how do you avoid skipping elements? 37 A) Use an enhanced for loop. B) Manually decrement the index i-- after removal. C) Clear the entire list first. D) Use a while loop that never increments. E) It is impossible. **Answer:** B 37 \#\#\#\# 8 Which line correctly prints names using for (Address addr : list)? 25 A) System.out.println(list\[addr\].getName()); B) System.out.println(Address.getName()); C) System.out.println(addr.getName()); D) System.out.println(list.getName()); E) System.out.println(addr); **Answer:** C 41 \#\#\#\# 9 A method uses an enhanced for loop to find the max ID. What is the **scope** of the loop variable student? 41, 43 A) The entire class. B) The whole method. C) Only the block of the enhanced for loop. D) Any subclass. E) Only the main method. **Answer:** C 36 \#\#\#\# 10 Why does the enhanced for loop **not** update a primitive value in ArrayList? 42, 44 A) ArrayLists are immutable. B) The variable is assigned a copy of the element; changing it does not affect the list. C) Primitives cannot be stored in ArrayLists. D) Wrapper classes do not allow modification. E) Integer objects are static. **Answer:** B 44 \--- \#\#\# **Topic 4.10 — Implementing ArrayList Algorithms** **题目数量：10** \#\#\#\# 1 (真题算法) A "compression" algorithm moves only non-zero elements to a new list. If the original is , what is the result? \[40, 45\] A)  B)  C)  D)  E)  **Answer:** A 46 \#\#\#\# 2 (真题逻辑) The averageWithinRange(10.0, 50.0) method is called on inventory \[20.0 (T), 45.0 (T), 10.0 (T), 45.0 (F)\]. What is returned? 47, 48 A) 20.0 B) 25.0 C) 30.0 D) 37.33 E) 45.0 **Answer:** B ( (20+45+10)/3 ) 47 \#\#\#\# 3 Which strategy determines if an ArrayList contains **duplicate** elements? 46 A) A single loop to check the first element. B) Using nested traversals or secondary storage (like a Set) to compare all elements. C) Sorting and returning the middle element. D) Summing elements and checking the size. E) Converting the list to a String. **Answer:** B 46 \#\#\#\# 4 Which method inserts an element at index, shifting subsequent elements to the right? 27, 49 A) set(index, obj) B) add(obj) C) add(index, obj) D) remove(index) E) insert(index, obj) **Answer:** C 49 \#\#\#\# 5 A standard algorithm to **reverse** an ArrayList is implemented by: 49 A) Calling list.size() and adding 1 to each index. B) Swapping elements from opposite ends until the middle is reached. C) Replacing all elements with null. D) Using an enhanced for loop to print. E) Adding a new element to the end. **Answer:** B 50 \#\#\#\# 6 (真题) Which algorithm requires multiple ArrayList objects to be traversed **simultaneously**? 48 A) Summing elements in a single list. B) Comparing elements at the same index in two different lists. C) Finding the maximum value in one list. D) Printing a single string. E) Counting characters in a word. **Answer:** B 48 \#\#\#\# 7 Which algorithm involves accessing all **consecutive pairs** of elements? 50 A) Finding the average. B) Reversing the list. C) Checking for specific properties between neighbors (e.g., if sorted). D) Initializing the list with new. E) Converting an int to Integer. **Answer:** C 48 \#\#\#\# 8 To compute the **average** of numerical values, the algorithm must: 48 A) Find the max and divide by 2\. B) Traverse the list to compute the sum, then divide by total count. C) Multiply all elements. D) Return the value at index 0\. E) Use Math.sqrt() on the size. **Answer:** B 51 \#\#\#\# 9 The algorithm to **shift or rotate** elements typically requires: 51 A) A single if statement. B) A traversal that moves elements to a new index or uses a temporary variable. C) Setting the list to null. D) Calling System.out.println. E) Using a boolean variable. **Answer:** B 51 \#\#\#\# 10 In an algorithm to find the **maximum** value, what should the max variable be initialized to? 52 A) 0 B) Integer.MAX\_VALUE C) The first element of the list. D) The last element of the list. E) \-1 **Answer:** C 52 \--- **类比 (Analogy):** 处理 **ArrayList (Topic 4.8–4.10)** 就像在一条**移动的传送带**上点货： \* **Topic 4.8** 是你学会如何抓取、替换或丢弃传送带上的盒子。 \* **Topic 4.9** 提醒你，如果你在传送带运行时拿走一个盒子，后面的盒子会自动向前滑，如果你不调整步子，就会漏检下一个盒子。 \* **Topic 4.10** 则是更复杂的物流任务，比如按重量重新排序或找出两个完全相同的包裹。  
